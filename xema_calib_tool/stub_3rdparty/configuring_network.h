#pragma once

// STUB -- not the real vendor header. The real XEMA-master/3rdparty/ConfiguringIP/ package
// is a separate prebuilt-binary bundle not present in this checkout. open_cam3d.cpp includes
// it but sdk/CMakeLists.txt doesn't link "enumerate" (only needed for device-discovery
// example targets, not the DfConnect/DfCaptureBrightnessData path this GUI uses), and this
// empty stub was confirmed sufficient in the previous build -- open_cam3d_sdk.dll built clean.
